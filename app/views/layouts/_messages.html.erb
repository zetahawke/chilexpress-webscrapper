<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> center-align">
    <a href="#" data-dismiss="alert" class="close" style="color: white;">X</a>
      <ul>
        <li>
          <%= value %>
        </li>
      </ul>
  </div>
<% end %>